# PRD: 제품 판매 기록 관리 웹앱

## 한 줄 목적

기획 MD가 혼자서, 브라우저만으로 제품 판매 기록을 입력·조회·집계할 수 있는 개인용 소형 웹앱.

## 사용자

- 여성 잡화 브랜드 기획 MD 1인 (개인 업무용, 다중 사용자 없음)

## 데이터 모델 참고

- **품번**: 연도·시즌에 따라 달라지는 개별 상품 식별 코드 (예: `24FW-A102-BK`, 컬러/사이즈별로 다름)
- **모코드**: 컬러·사이즈 등 변형을 아우르는 상위 스타일 식별 코드 (예: `A102`). 여러 품번이 하나의 모코드에 묶임
- 매출/판매량 집계는 **모코드 기준**으로 볼 수 있어야 함 (동일 모코드의 여러 품번 판매를 합산)

## 핵심 기능 (5개)

1. **판매 기록 등록** — 연도, 시즌(SS/FW), 품번, 모코드, 제품명, 수량, 단가, 판매일, 판매채널(자사몰/오프라인/위탁 등)을 입력해 기록 추가
2. **판매 기록 목록 조회/검색** — 등록된 기록을 목록으로 보고, 기간·제품명·품번·모코드·시즌·판매채널로 검색·필터링
3. **판매 기록 수정/삭제** — 잘못 입력한 기록을 수정하거나 삭제
4. **매출 통계 요약** — 총매출, 기간별(일/월) 매출 합계, 채널별 매출 비중, **모코드별 판매량·매출 집계**(동일 모코드의 여러 품번 합산)를 대시보드 형태로 요약
5. **데이터 내보내기(CSV)** — 전체 판매 기록을 CSV 파일로 다운로드 (백업 용도)

## 화면 구성 (대략)

- **대시보드 화면**: 총매출/기간별 요약 통계 카드 + 모코드별 판매량·매출 순위 + 최근 판매 기록 일부
- **판매 기록 목록 화면**: 전체 기록 테이블(검색/필터 포함), 각 행에서 수정·삭제, 상단에 CSV 내보내기 버튼
- **기록 등록/수정 화면(또는 모달)**: 연도, 시즌, 품번, 모코드, 제품명, 수량, 단가, 판매일, 판매채널 입력 폼

모바일에서도 조회·입력이 가능하도록 반응형으로 구성.

## 저장 방식

- 브라우저 **localStorage**에 JSON 형태로 저장 (서버/DB 없음, 로그인 없음)
- 데이터는 기기·브라우저별로 독립적으로 저장됨 (다른 기기와 자동 동기화되지 않음)

## 이번엔 안 만들 것 (범위 밖)

- 로그인/회원가입, 다중 사용자 지원
- 서버 저장 또는 기기 간 자동 동기화(클라우드)
- CSV 가져오기(import) — 이번엔 내보내기(export)만 지원
- 재고 관리, 매입가/원가 및 순이익 계산
- 알림/리마인더 기능
- 다국어 지원
